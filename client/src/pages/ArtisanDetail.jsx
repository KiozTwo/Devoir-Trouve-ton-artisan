import {useEffect,useState} from 'react';import {Link,useParams} from 'react-router-dom';import {api} from '../api';import {Stars} from '../components/ArtisanCard';
const empty={name:'',email:'',subject:'',message:'',website:''};
export default function ArtisanDetail(){const {id}=useParams(),[a,setA]=useState(null),[form,setForm]=useState(empty),[status,setStatus]=useState(''),[error,setError]=useState('');useEffect(()=>{api.artisan(id).then(x=>{setA(x);document.title=`${x.name} | Trouve ton artisan`;document.querySelector('meta[name=description]').content=`Contactez ${x.name}, ${x.specialty.name} à ${x.city}.`}).catch(e=>setError(e.message))},[id]);async function submit(e){e.preventDefault();setStatus('Envoi en cours…');try{const result=await api.contact(id,form);setForm(empty);setStatus(result.message)}catch(e){setStatus(e.message)}}if(error)return <section className="container section"><h1>Artisan introuvable</h1><p>{error}</p><Link to="/">Retour à l’accueil</Link></section>;if(!a)return <p className="container section" role="status">Chargement…</p>;return <section className="container section detail"><div className="profile"><div className="avatar large" aria-hidden="true">{a.name[0]}</div><div><p className="eyebrow">{a.specialty.category.name}</p><h1>{a.name}</h1><Stars value={a.rating}/><p>{a.specialty.name} · {a.city}</p>{a.website&&<a href={a.website} target="_blank" rel="noopener noreferrer">Visiter le site web</a>}</div></div><div className="detail-grid"><article><h2>À propos</h2><p>{a.about}</p></article><form className="contact-card" onSubmit={submit}><h2>Contacter l’artisan</h2><p>Tous les champs sont obligatoires.</p><label htmlFor="name">Nom</label><input id="name" className="form-control" required minLength="2" maxLength="100" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><label htmlFor="email">E-mail</label><input id="email" className="form-control" type="email" required maxLength="254" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><label htmlFor="subject">Objet</label><input id="subject" className="form-control" required minLength="3" maxLength="150" value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})}/><label htmlFor="message">Message</label><textarea id="message" className="form-control" required minLength="10" maxLength="3000" rows="5" value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/><div className="honeypot" aria-hidden="true"><label htmlFor="website">Site web</label><input id="website" tabIndex="-1" autoComplete="off" value={form.website} onChange={e=>setForm({...form,website:e.target.value})}/></div><button className="btn btn-primary" type="submit">Envoyer le message</button>{status&&<p className="form-status" role="status">{status}</p>}</form></div></section>}

