import {useEffect,useState} from 'react';import {useLocation,useParams} from 'react-router-dom';import {api} from '../api';import ArtisanCard from '../components/ArtisanCard';
export default function ArtisanList(){const {slug}=useParams();const loc=useLocation();const q=new URLSearchParams(loc.search).get('q')||'';const [items,setItems]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');const title=q?`Résultats pour « ${q} »`:slug?.replace(/-/g,' ');useEffect(()=>{setLoading(true);setError('');const p=q?`?search=${encodeURIComponent(q)}`:`?category=${encodeURIComponent(slug||'')}`;api.artisans(p).then(setItems).catch(e=>setError(e.message)).finally(()=>setLoading(false));document.title=`${title||'Artisans'} | Trouve ton artisan`},[slug,q]);return <section className="container section"><h1 className="text-capitalize">{title||'Nos artisans'}</h1>{loading&&<p role="status">Chargement…</p>}{error&&<p className="alert alert-danger" role="alert">{error}</p>}{!loading&&!error&&items.length===0?<p>Aucun artisan ne correspond à votre recherche.</p>:<div className="cards">{items.map(a=><ArtisanCard key={a.id} artisan={a}/>)}</div>}</section>}
