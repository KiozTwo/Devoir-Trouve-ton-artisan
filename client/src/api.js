const BASE=(import.meta.env.VITE_API_URL||'http://localhost:3001/api').replace(/\/$/,'');
async function request(path,options={}){const r=await fetch(`${BASE}${path}`,{headers:{'Content-Type':'application/json',...(options.headers||{})},...options});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.message||'Une erreur est survenue.');return data;}
export const api={categories:()=>request('/categories'),artisans:(params='')=>request(`/artisans${params}`),artisan:id=>request(`/artisans/${id}`),contact:(id,body)=>request(`/artisans/${id}/contact`,{method:'POST',body:JSON.stringify(body)})};
