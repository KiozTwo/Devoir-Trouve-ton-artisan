# Sécurité et veille

La liste détaillée des mesures, leur intérêt et le protocole de veille sont intégrés aux sections 7 et 8 de `DOSSIER_FINAL.md`. Avant remise : exécuter `npm audit` dans la racine, `client` et `server`, consigner la date et le résultat, corriger les vulnérabilités exploitables, vérifier les en-têtes de production et effectuer un test OWASP ZAP passif sur l'URL déployée.
